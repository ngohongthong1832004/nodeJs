{{!-- 
<h1>{{#each course}}</h1>

<h2>{{cost}}</h2>
<h2>{{name}}</h2>
<h2>{{description}}</h2> --}}

<div style="display: flex;">
    {{#each course}}
    <div style="width: 300px; height:300px; border: 1px solid black; padding : 10px">
            <a href="/course/{{this.slug}}">{{this.slug}}</a>
        {{!-- {{#each this}} --}}
            <li>{{this.name}}</li>
            <li>{{this.cost}}</li>
            <li>{{this.description}}</li>
        {{!-- {{/each}} --}}
    </div>
    {{/each}}
</div>
